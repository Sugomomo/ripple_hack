# ripple_hack
# Quick Start
## Run backend server
Sync uv packages with:
```
uv sync
```

In the backend directory, run the following command:
```
uv run uvicorn main:app --reload
```

## Run frontend server
Install the required dependencies first:
```
npm install
```

In the frontend directory, run the following command:
```
npm run dev
```
